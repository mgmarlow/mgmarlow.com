<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>{{ title }}</title>
    <meta name="description" content="Graham Marlow on the internet"/>
    <link rel="icon" type="image/x-icon" href="/favicon.ico"/>
    <link href="{{ '/index.css' | url }}" rel="stylesheet"/>
    <link href="https://unpkg.com/prismjs@1.20.0/themes/prism-tomorrow.css" rel="stylesheet">
    <script src="{{ '/bundle.js' | url }}"></script>
    <style>
      /* Match tailwind typography defaults */
      pre[class*="language-"] {
        font-size: 0.78em;
      }
    </style>
  </head>
  <body class="bg-zinc-50 dark:bg-zinc-800 text-slate-500 dark:text-gray-100">
    <div class="mt-6 px-3">
      {% include "header.njk" %}
    </div>
    
    <div class="max-w-prose mx-auto my-8 md:my-16 p-3 md:p-0">
      <main>
        {% block content %}
          {{ content | safe }}
        {% endblock %}
      </main>

      {% block footer %}
      {% endblock %}
    </div>
  </body>
</html>
