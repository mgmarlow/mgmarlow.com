---
layout: layouts/page.njk
---

{%- for post in collections.post | reverse | head(10) -%}
  <article class="mobile-block spacing-4">
    <h3 class="m-0"><a href="{{ post.url | url }}">{{ post.data.title }}</a></h3>
    <p class="m-0"><small><time>{{ post.data.date | postDate }}</time></small></p>
    <p>{{ post.data.description }}</p>
    <p><a href="{{ post.url | url }}">Continue reading →</a></p>
  </article>
{%- endfor -%}

<p>→ <a href="{{ '/archive' | url }}">More in the archive</a></p>
